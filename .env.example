# ─── 数据目录（文件存储模式）──────────────────────────────────────────────
# Amber 数据存储根目录（捕获记录 JSON + 本地 blob 文件）
# 默认值：./amber-data
# AMBER_DATA_DIR=./amber-data

# ─── PostgreSQL（可选，设置后替代文件存储）────────────────────────────────
# 设置此变量后，Capture 数据将写入 Postgres，否则使用本地 JSON 文件
# 迁移：运行 npx prisma migrate deploy（或 prisma db push）初始化 schema
# DATABASE_URL=postgres://user:password@localhost:5432/amber
#
# Supabase：统一用 Session Pooler（端口 5432），一个串同时满足建表和运行时
# 密码含特殊字符需 URL 编码：node -e "console.log(encodeURIComponent('密码'))"
# 详见 docs/configuration.md → 使用 Supabase
# DATABASE_URL=postgresql://postgres.<project-ref>:<编码后的密码>@aws-1-<region>.pooler.supabase.com:5432/postgres

# ─── Cloudflare R2（可选，设置后替代本地 blob 存储）──────────────────────
# 前 4 个变量（ACCOUNT_ID/ACCESS_KEY_ID/SECRET_ACCESS_KEY/BUCKET）同时设置时启用
# R2BlobStore，缺少任何一个则回退到本地文件。R2_PUBLIC_BASE_URL 不参与启用判断，
# 但强烈建议设置——它是生成图片/视频公开 URL 的前缀，留空会导致 blob 加载异常。
# R2_ACCOUNT_ID=your_cloudflare_account_id
# R2_ACCESS_KEY_ID=your_r2_access_key_id
# R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
# R2_BUCKET=amber-blobs
# R2_PUBLIC_BASE_URL=https://your-r2-public-domain.com

# ─── blob 引用迁移（amber migrate-blob-refs 命令用）───────────────────────
# 若历史 capture 正文里存的是带此前缀的完整 URL（如 http://localhost:7788/blobs/...），
# migrate-blob-refs 会连同此前缀一起反解析成 amber-asset: 稳定引用。
# 留空则只处理相对路径 /blobs/... 形态。
# AMBER_PUBLIC_BASE_URL=

# ─── Web 服务端口（可选）─────────────────────────────────────────────────
# 默认值：3000
# PORT=3000

# ─── 集成测试用 Postgres（开发者本地测试）────────────────────────────────
# 启动：docker compose -f packages/adapters/docker-compose.test.yml up -d
# TEST_DATABASE_URL=postgres://amber:amber@localhost:5433/amber_test
